apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: laravel
spec:
  storageClassName: storage-class-laravel
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi